{
  "name": "api",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node --log-error' src/index.ts",
    "run": "node dist/index.js",
    "build:watch": "npm run build -- --watch --preserveWatchOutput",
    "build": "tsc"
  },
  "dependencies": {
    "apollo-server": "^2.11.0",
    "graphql": "^14.6.0"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "kill-port": "^1.6.0",
    "nodemon": "^2.0.2",
    "ts-node": "^8.6.2",
    "typescript": "^3.8.3"
  }
}
