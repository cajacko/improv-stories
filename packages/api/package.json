{
  "name": "api",
  "version": "0.1.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "start": "npm run build && concurrently \"npm run build:watch\" \"nodemon dist/index.js\"",
    "run": "node dist/index.js",
    "build:watch": "npm run build --watch",
    "build": "tsc"
  },
  "dependencies": {
    "apollo-server": "^2.11.0",
    "graphql": "^14.6.0"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2",
    "typescript": "^3.8.3"
  }
}
