{
  "name": "improv-stories-app",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lerna": "lerna publish",
    "init": "lerna init",
    "test:lint": "eslint --ext .js --ext .jsx packages",
    "test": "yarn test:lint"
  },
  "files": [
    "lib/**/*"
  ],
  "devDependencies": {
    "lerna": "^2.5.1"
  }
}
